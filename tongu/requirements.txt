# KEadapter 패키지 의존성

# 비동기 HTTP 클라이언트
aiohttp>=3.8.0
aiofiles>=22.1.0

# 진행 상황 표시
tqdm>=4.64.0

# 재시도 로직
backoff>=2.2.0

# 데이터 처리
typing-extensions>=4.3.0

# 로깅 (표준 라이브러리 사용)
# json, pickle, hashlib, re, pathlib, asyncio, logging은 표준 라이브러리